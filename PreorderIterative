   ArrayList<Integer> preOrder(Node root1)
    {
        // Code
        ArrayList<Integer> a=new ArrayList<>();
           Stack<Node> st=new Stack<>();
	    st.push(root1);
	    while(st.size()>0)
	    {
	       
	       Node root=st.pop();
	        if(root!=null)
	        {
	            a.add(root.data);  // Here use print to print the data
	            if(root.right!=null)
	            st.push(root.right);
	            if(root.left!=null)
	             st.push(root.left);
	       }
	    }
	    return a;
        }
        
        
        TC-O(n)
        SC_-O(n) for skew otherwise height
